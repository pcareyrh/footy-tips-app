services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - db-data:/app/backend/prisma
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped

volumes:
  db-data:
